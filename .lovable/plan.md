Hide the "Edit with Lovable" badge on the published Cognarah site by calling `publish_settings--set_badge_visibility` with `hide_badge: true`. No code changes. After approval, the badge stops appearing on the next published deployment.